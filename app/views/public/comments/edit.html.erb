
<h2>Editing Comment</h2>

<%= form_for [:public,@post,@comment],url: public_post_comment_path(@post,@comment) do |f| %>

	<% if @comment.errors.any? %>
    	<div class="text-danger">
      	<ul>
        <h3><%= @comment.errors.count %>errors prohibited this user from being saved:</h3>
      	</ul>
      	<ul>
        	<% @comment.errors.full_messages.each do |msg| %>
        		<p><%= msg %></p>
        	<% end %>
      	</ul>
    	</div>
  	<% end %>

  <h4>Comment</h4>
  <%= f.text_field :conversation %>

  <h4><%= f.submit 'Update Comment', class:"btn btn-warning" %></h4>

<% end %>

	<%= link_to "Back", public_post_path(@post.id) %>

</div>
</div>
</div>