<!– フォロー解除ボタン –>
<%= form_for [:public, current_user.following_relationships.find_by(following_id: @user.id)], html: { method: :delete } do |f| %>
  <%= f.submit "フォローを解除する", class: "btn btn-sm btn-secondary" %>
<% end %>