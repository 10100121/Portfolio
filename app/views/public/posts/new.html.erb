<h1>投稿ページ</h1>
<%= form_for(@post, url: public_posts_path) do |f| %>

	<h4>タイトル</h4>
	<%= f.text_field :title%>

	<h4>内容</h4>
	<%= f.text_field :body%>

	<%= f.submit "投稿", class:"btn btn-lg btn-block btn-base-1 mt-20" %>

<% end %>